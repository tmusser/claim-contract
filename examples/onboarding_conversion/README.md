# Onboarding conversion trap

The aggregate post-launch metric is 8% higher than the pre-launch metric. Traffic composition also changed, and the design is observational.

The proposed sentence uses causal language. The harness therefore blocks it and separately requests review for composition stability, uncertainty, and the missing non-causal caveat.

Run:

```bash
claim-contract validate examples/onboarding_conversion/contract.yaml
```

The example does not prove the redesign had no effect. It shows that the submitted evidence contract does not support the proposed causal language.
